apiVersion: v1
kind: Service
metadata:
  name: codemonfront-web
  labels:
    app: codemonfront-web
spec:
  type: ClusterIP
  selector:
    app: codemonfront-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
