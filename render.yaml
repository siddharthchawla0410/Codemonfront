services:
  - type: web
    name: codemonfront
    env: docker
    dockerfilePath: ./Dockerfile
    port: 10000
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://codemonbackend.onrender.com
